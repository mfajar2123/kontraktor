allowBuilds:
  # Dibutuhkan oleh Nuxt/Vite pada instalasi bersih di CI (termasuk Netlify).
  # Tetap gunakan allow-list agar build script paket lain tidak otomatis berjalan.
  '@parcel/watcher': true
  esbuild: true
  sharp: true
  vue-demi: true
